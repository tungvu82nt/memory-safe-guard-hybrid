# Memory Safe Guard - Deployment Guide\n\n## 🚀 Hướng dẫn Deploy\n\n### Bước 1: Chuẩn bị Repository\n\n1. **Fork hoặc Clone repository này**\n   ```bash\n   git clone https://github.com/tungvu82nt/memory-safe-guard-hybrid.git\n   cd memory-safe-guard-hybrid\n   ```\n\n2. **Cài đặt dependencies**\n   ```bash\n   npm install\n   ```\n\n3. **Test local**\n   ```bash\n   npm run dev\n   ```\n   Truy cập http://localhost:8080 để kiểm tra\n\n### Bước 2: Build Production\n\n```bash\nnpm run build\n```\n\nThư mục `dist` sẽ chứa các file production ready.\n\n### Bước 3: Deploy Options\n\n#### Option 1: Static Hosting Services\n\n**Vercel:**\n1. Kết nối GitHub repository\n2. Auto-deploy từ main branch\n3. Build command: `npm run build`\n4. Output directory: `dist`\n\n**GitHub Pages:**\n1. Enable GitHub Pages trong repository settings\n2. Sử dụng GitHub Actions để auto-build và deploy\n3. Tạo workflow file `.github/workflows/deploy.yml`\n\n**Firebase Hosting:**\n```bash\nnpm install -g firebase-tools\nfirebase login\nfirebase init hosting\nfirebase deploy\n```\n\n#### Option 2: VPS/Server Deployment\n\n**Nginx Configuration:**\n```nginx\nserver {\n    listen 80;\n    server_name your-domain.com;\n    root /path/to/dist;\n    index index.html;\n    \n    location / {\n        try_files $uri $uri/ /index.html;\n    }\n}\n```\n\n**Apache Configuration:**\n```apache\n<VirtualHost *:80>\n    ServerName your-domain.com\n    DocumentRoot /path/to/dist\n    \n    <Directory /path/to/dist>\n        Options Indexes FollowSymLinks\n        AllowOverride All\n        Require all granted\n        \n        RewriteEngine On\n        RewriteBase /\n        RewriteRule ^index\\.html$ - [L]\n        RewriteCond %{REQUEST_FILENAME} !-f\n        RewriteCond %{REQUEST_FILENAME} !-d\n        RewriteRule . /index.html [L]\n    </Directory>\n</VirtualHost>\n```\n\n### Bước 4: Cấu hình SPA Routing\n\nĐể React Router hoạt động đúng, cần cấu hình server redirect tất cả routes về `index.html`:\n\n**Tạo file `public/_redirects` (cho các static hosting):**\n```\n/*    /index.html   200\n```\n\n**Hoặc file `.htaccess` (cho Apache):**\n```apache\nOptions -MultiViews\nRewriteEngine On\nRewriteCond %{REQUEST_FILENAME} !-f\nRewriteRule ^ index.html [QSA,L]\n```\n\n## 🔧 Environment Variables\n\nKhông cần environment variables cho phiên bản IndexedDB thuần túy.\n\nNếu muốn thêm API backend sau này:\n```\nVITE_API_URL=https://your-api-domain.com\nVITE_APP_ENV=production\n```\n\n## 📊 Performance Optimization\n\n### Build Optimization\n- ✅ Vite build optimization\n- ✅ Tree shaking\n- ✅ Code splitting\n- ✅ Asset optimization\n- ✅ Gzip compression\n\n### Runtime Optimization\n- ✅ IndexedDB caching\n- ✅ Lazy loading components\n- ✅ Debounced search\n- ✅ Memoized calculations\n\n## 🔒 Security Features\n\n- ✅ Client-side only (không có server)\n- ✅ IndexedDB local storage\n- ✅ No external API calls\n- ✅ HTTPS enforced (khuyến nghị)\n- ✅ CSP headers (khuyến nghị)\n\n## 📱 PWA Support (Future)\n\nCó thể thêm PWA support:\n```bash\nnpm install vite-plugin-pwa\n```\n\n## 🐛 Troubleshooting\n\n### Build Errors\n```bash\n# Clear cache\nrm -rf node_modules package-lock.json\nnpm install\nnpm run build\n```\n\n### Runtime Errors\n- Kiểm tra browser console\n- Kiểm tra IndexedDB support\n- Clear browser cache\n- Kiểm tra HTTPS requirements\n\n## 📈 Monitoring\n\n### Performance Monitoring\n- Google Analytics (optional)\n- Web Vitals tracking\n- Error tracking services\n\n### Browser DevTools\n- IndexedDB inspection\n- Performance profiling\n- Network monitoring\n\n## 🔄 CI/CD Pipeline Example\n\n**GitHub Actions (.github/workflows/deploy.yml):**\n```yaml\nname: Deploy to GitHub Pages\n\non:\n  push:\n    branches: [ main ]\n\njobs:\n  build-and-deploy:\n    runs-on: ubuntu-latest\n    steps:\n    - uses: actions/checkout@v2\n    \n    - name: Setup Node.js\n      uses: actions/setup-node@v2\n      with:\n        node-version: '18'\n        \n    - name: Install dependencies\n      run: npm install\n      \n    - name: Build\n      run: npm run build\n      \n    - name: Deploy to GitHub Pages\n      uses: peaceiris/actions-gh-pages@v3\n      with:\n        github_token: ${{ secrets.GITHUB_TOKEN }}\n        publish_dir: ./dist\n```\n\n## 📞 Support\n\nNếu gặp vấn đề:\n1. Check [Issues](https://github.com/tungvu82nt/memory-safe-guard-hybrid/issues)\n2. Create new issue với logs chi tiết\n3. Contact: tungvu82nt@gmail.com\n\n---\n\n**Repository**: https://github.com/tungvu82nt/memory-safe-guard-hybrid"